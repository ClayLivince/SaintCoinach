<Window x:Class="Godbert.Views.RemarkDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Add remark" Width="420" SizeToContent="Height"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize" ShowInTaskbar="False">
    <StackPanel Margin="14">
        <TextBlock x:Name="HeaderText" FontWeight="Bold" Margin="0,0,0,10" />

        <TextBlock Text="Label (shown in the set list; searchable)" Margin="0,0,0,2" />
        <TextBox x:Name="LabelBox" />

        <TextBlock Text="Note" Margin="0,10,0,2" />
        <TextBox x:Name="NoteBox" AcceptsReturn="True" TextWrapping="Wrap"
                 Height="90" VerticalScrollBarVisibility="Auto" />

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="Save" Width="84" IsDefault="True" Click="Save_Click" />
            <Button Content="Cancel" Width="84" Margin="8,0,0,0" IsCancel="True" Click="Cancel_Click" />
        </StackPanel>
    </StackPanel>
</Window>
